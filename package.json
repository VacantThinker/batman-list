{
  "name": "batman-list",
  "version": "0.1.3",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "rmdir /q /s .next && next build && next export && type nul > out/.nojekyll ",
    "deploy": "npm run export && gh-pages -t -d out",
    "start": "next build && next start"
  },
  "dependencies": {
    "next": "9.1.2",
    "react": "16.11.0",
    "react-dom": "16.11.0"
  },
  "devDependencies": {
    "axios": "^0.19.0",
    "gh-pages": "^2.1.1",
    "prettier": "^1.18.2"
  }
}
